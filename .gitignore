# Go build output
bin/
dist/

# Editor / IDE
.opencode
.planning
# v1 planning archive (moved from .planning/; local only, never commit)
.planning.*/

# Scratch
tmp/
_test_*

# GSD-Lite project management artifacts (local only — never commit to public remote)
gsd-lite/