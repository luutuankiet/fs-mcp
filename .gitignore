# Go build output
bin/
dist/

# Editor / IDE
.opencode
.planning
.planning.*/   # v1 planning archive (local only — never commit)

# Scratch
tmp/
_test_*

# GSD-Lite project management artifacts (local only — never commit to public remote)
gsd-lite/